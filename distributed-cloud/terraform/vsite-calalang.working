# Virtual Site  for Generic HTTP

resource "volterra_virtual_site" "vsite-calalang-volt-rg" {
  name      = "vsite-calalang-volt-rg"
  namespace = "staging"

  site_selector {
    expressions = ["region in (us-west1, us-west2),tier in (staging)"]
  }

  site_type = ["CUSTOMER_EDGE"]
}