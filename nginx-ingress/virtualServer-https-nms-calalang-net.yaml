apiVersion: k8s.nginx.org/v1
kind: VirtualServer
metadata:
  name: nms.calalang.net
  namespace: nms
spec:
  host: nms.calalang.net
  tls:
    secret: nms-apigw-certs
    redirect:
      enable: true
  upstreams:
  - name: apigw
    service: apigw
    port: 443
    type: http
  routes:
  - path: /
    location-snippets: |
      status_zone nms-server;
    policies:
    - name: oidc-policy
    action:
      proxy:
        upstream: apigw