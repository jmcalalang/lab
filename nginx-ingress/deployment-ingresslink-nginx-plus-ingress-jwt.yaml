apiVersion: apps/v1
kind: Deployment
metadata:
  name: ingresslink-nginx-ingress
  namespace: nginx-ingress
spec:
  selector:
    matchLabels:
      app: ingresslink-nginx-ingress
  template:
    metadata:
      labels:
        app: ingresslink-nginx-ingress
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "9113"
    spec:
      serviceAccountName: nginx-ingress
      containers:
      - image: private-registry.nginx.com/nginx-ic-nap/nginx-plus-ingress:2.4.2
        imagePullPolicy: Always       
        name: nginx-plus-ingress
        ports:
        - name: http
          containerPort: 80
          hostPort: 80
        - name: https
          containerPort: 443
          hostPort: 443
        - name: prometheus
          containerPort: 9113
        - name: readiness-port
          containerPort: 8081
        readinessProbe:
          httpGet:
            path: /nginx-ready
            port: readiness-port
          periodSeconds: 1
        securityContext:
          allowPrivilegeEscalation: true
          runAsUser: 101 #nginx
          capabilities:
            drop:
            - ALL
            add:
            - NET_BIND_SERVICE
        env:
        - name: POD_NAMESPACE
          valueFrom:
            fieldRef:
              fieldPath: metadata.namespace
        - name: POD_NAME
          valueFrom:
            fieldRef:
              fieldPath: metadata.name
        args:
          - -nginx-plus
          - -nginx-configmaps=$(POD_NAMESPACE)/ingresslink-nginx-config
          - -default-server-tls-secret=$(POD_NAMESPACE)/default-server-secret
          - -global-configuration=$(POD_NAMESPACE)/nginx-configuration
          - -enable-prometheus-metrics
          - -enable-latency-metrics
          - -ingresslink=ingresslink-nginx-ingress
          - -report-ingress-status
          - -enable-tls-passthrough
          - -enable-oidc
          - -enable-custom-resources
          - -ingress-class=ingresslink-nginx
          #- -enable-internal-routes
      imagePullSecrets:
        - name: regcred