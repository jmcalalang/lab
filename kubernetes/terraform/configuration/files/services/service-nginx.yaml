apiVersion: v1
kind: Service
metadata:
  name: nginx-service
  namespace: terraform
  labels:
    app: nginx-service
spec:
  ports:
  - name: http
    port: ${nginx_image}
  selector:
    app: nginx-deployment
  type: ClusterIP