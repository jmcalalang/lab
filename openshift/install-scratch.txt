Openshift:
    oc create secret generic bigip-login -n kube-system --from-literal=username=admin --from-literal=password=Harrison123!
    oc apply -f https://raw.githubusercontent.com/F5Networks/k8s-bigip-ctlr/master/docs/config_examples/ingress/networkingV1/example-default-ingress-class.yaml
    oc apply -f https://raw.githubusercontent.com/F5Networks/k8s-bigip-ctlr/master/docs/config_examples/customResourceDefinitions/customresourcedefinitions.yaml
    oc apply -f https://raw.githubusercontent.com/F5Networks/k8s-bigip-ctlr/master/docs/config_examples/rbac/clusterrole.yaml
    oc create serviceaccount bigip-ctlr -n kube-system
    oc adm policy add-cluster-role-to-user cluster-admin -z bigip-ctlr -n kube-system
    oc create -f f5-kctlr-openshift-hostsubnet.yaml

BIG-IP:

    create net tunnels vxlan vxlan-mp flooding-type multipoint
        # Self-IP Address of BIG-IP for vtep
    create net tunnels tunnel openshift_vxlan key 0 profile vxlan-mp local-address 10.0.3.110
        # IP address in Host subenet
    create net self 10.130.2.83/23 allow-service all vlan openshift_vxlan
    create auth partition openshift

CIS:

    oc apply -f f5-k8s-bigip-ctlr-openshift.yaml

Demo Routes:
    oc apply -f https://raw.githubusercontent.com/F5Networks/k8s-bigip-ctlr/master/docs/config_examples/routes/sample-route-basic.yaml
    oc delete -f https://raw.githubusercontent.com/F5Networks/k8s-bigip-ctlr/master/docs/config_examples/routes/sample-route-basic.yaml