# Ansible Variables

# Use Ansible module or skip
state: "present"
#state: 'absent'

# Ansible Date Time
ansible_date_time: "{{ lookup('pipe', 'date +%Y-%m-%d.%H:%M:%S') }}"

# NGINX One Variables
namespace: jmcalalang

# List of NGINX One Instance IDs
config_sync_group_nginx_azure_instances: 
  - config_sync_group: csg_7CrclNk0TDeOezQsJZvYqQ