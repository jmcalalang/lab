# Ansible Variables

# Use Ansible module or skip
state: "present"
#state: 'absent'

# Ansible Date Time
ansible_date_time: "{{ lookup('pipe', 'date +%Y-%m-%d.%H:%M:%S') }}"

# List of NGINX One Instance IDs
azure_instances: 
  - instance: inst_pBw1xbmTT7CKnqGh5p5kzA
  - instance: inst_mDlsy9LlQ8mRaXleuJuN0w